#include<bits/stdc++.h>
using namespace std;

int main() {
		vector<string> names;     // Declaring of vector
		names.push_back("Anuj");
		for(string s : names) {
				cout << s << endl;
		}
		return 0;
}