//  Initialization using a single value

#include<bits/stdc++.h>
using namespace std;

int main() {
		vector<string> names(3, "ADx");

		for(string s : names) {
			cout << s << endl;
		}

		return 0;
}